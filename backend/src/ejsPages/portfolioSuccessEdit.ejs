<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="ITConnect" content="width=device-width, initial-scale=1.0">
    <title>"ITConnect"</title>
    <link rel="stylesheet" href="libs/bootstrap-reboot.min.css">
    <link rel="stylesheet" href="libs/bootstrap-grid.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin >
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <!-- Подключение внешнего CSS-файла -->
    <link rel="stylesheet" href="/css/redactor-achievements.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>
<body>
<%- include('portfolioHeader'); -%>
<main class="page-main">
    <div class="flow1">
        <div class="container">
            <% if(user.description.avatar) {%>
                <img src="data:image/jpeg;base64,<%= user.description.avatar %>" alt="Profile Picture" id="profilePic" class="users item">
            <% } else {%>
                <img src="/img/users.png" alt="Profile Picture" id="profilePic" class="users item">
            <% } %>
            <style>
                .users{
                    border-radius: 55px;
                }
            </style>
            <div class="container_1">
                <div class="item">
                    <a href="/description.html" class="item1 description">Описание</a>
                </div>
                <div class="item">
                    <a href="/redactor-contact.html" class="item1 contact-link">Контактная информация</a>
                    <input type="tel" class="contact-input" style="display: none;" placeholder="+7">
                </div>
                <div class="item">
                    <a href="#" class="item1 achievements-link">Достижения</a>
                    <input type="file" class="achievements-input" style="display: none;">
                </div>
                <div class="item">
                    <a href="/redactor-project.html" class="item1 projects-link">Проекты</a>
                </div>
                <div class="line"></div>
            </div>
            <div class="name-old">
                <button class="name" id="name"><span><%=user.name%></span></button>
                <button class="old" id="old"><span>Возраст: <%=user.contacts.age%></span></button>
            </div>
            <div class="favorite">
                <img src="" alt="Profile Picture" id="profilePic" class="users item" style="display: none;">
                <input type="file" id="imageInput" style="display: none;">
                <button class="change-images item" id="editButton" ><span>Изменить изображения</span></button>
                <button class="save item" id="saveButton" ><span>Сохранить изменения</span></button>
                <button class="cancel item" id="editButton" ><span>Отменить изменения</span></button>
                <script>
                    $('#imageInput').change(function() {
                        var reader = new FileReader();

                        reader.onload = function(e) {
                            $('#profilePic').attr('src', e.target.result);
                            $('#profilePic').show(); // Отображаем изображение
                            $('#profilePic').css('border-radius', '55px'); // Добавляем border-radius
                        }

                        reader.readAsDataURL(this.files[0]);
                    });

                    $(document).ready(function() {
                        // Кнопка "Изменить изображение"
                        $('#editButton').click(function() {
                            $('#imageInput').trigger('click');
                        });

                        // Обработка выбора файла
                        $('#imageInput').change(function() {
                            var reader = new FileReader();

                            reader.onload = function(e) {
                                $('#profilePic').attr('src', e.target.result);
                                $('#profilePic').show(); // Отображаем изображение
                            }

                            reader.readAsDataURL(this.files[0]);
                        });

                        // Кнопка "Сохранить изменения"
                        $('#saveButton').click(async function() {
                            document.getElementById('saveButton').setAttribute('disabled', 'disabled');
                            var formData = new FormData();
                            formData.append('avatar', $('#imageInput')[0].files[0]);

                            await fetch('/portfolio/avatar', {
                                method: 'PUT',
                                body: formData
                            })
                                .then(response => {
                                    if (response.ok) {
                                        console.log('super')
                                    } else {
                                        throw new Error('Server response was not ok');
                                    }
                                })
                                .catch(error => {
                                    console.log(error)
                                });
                        });
                    });
                </script>
            </div>
        </div>
    </div>
    </div>
    <div class="container-sertificat">
        <input type="file" id="sertificatInput" style="display: none;" class="srt-user">
        <div class="img-sertificat">
            <%for(let certificate of user.success.fixedCertificates) {%>
                <img src="data:image/jpeg;base64,<%= certificate %>" width="242" height="171" alt="Profile Picture">
            <%}%>
        </div>
        <div class="btn-srt">
            <button class="btn-new"><div class="txt-btn">Добавить сертификат</div></button>
        </div>
    </div>
    <script>
        $(document).ready(function() {
            $('.btn-new').click(function() {
                $('#sertificatInput').trigger('click');
            });

            $('#sertificatInput').change(async function() {
                var file = this.files[0];
                var reader = new FileReader();

                reader.onload = function(e) {
                    var newImg = $('<img>');
                    newImg.attr('src', e.target.result);
                    newImg.css({
                        'width': '242px',
                        'height': '171px'
                    });
                    $('.img-sertificat').prepend(newImg);
                };
                reader.readAsDataURL(file);
                let formData = new FormData();
                formData.append('certificate', $('#sertificatInput')[0].files[0]);
                document.getElementById('saveButton').setAttribute('disabled', 'disabled');
                await fetch('/portfolio/notFixedCertificate', {
                    method: 'POST',
                    body: formData
                })
                    .then(response => {
                        if (response.ok) {
                            console.log('super')
                            alert('Сертификат успешно загружен!')
                        } else {
                            throw new Error('Server response was not ok');
                        }
                    })
                    .catch(error => {
                        console.log(error)
                        alert('Сертификат не загружен, произошла ошибка')
                    });
                document.getElementById('saveButton').removeAttribute('disabled')
            });
        });
    </script>
</main>
<script src="/js/portfolioSuccessEdit.js"></script>
<%- include('portfolioFooter'); -%>
</body>